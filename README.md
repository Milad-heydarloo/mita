# Menumita Static Site Template

یک قالب مدرن و سئو-دوست برای وب‌سایت‌های استاتیک به زبان فارسی با پشتیبانی کامل از RTL.

## ✨ ویژگی‌ها

- 🎨 طراحی مدرن و واکنش‌گرا
- 🔍 بهینه‌سازی شده برای موتورهای جستجو (SEO)
- 🌐 کاملاً فارسی با پشتیبانی RTL
- 📱 طراحی واکنش‌گرا برای تمامی دستگاه‌ها
- ⚡ عملکرد سریع با بارگذاری تنبل تصاویر
- 🎯 انیمیشن‌های جذاب و حرفه‌ای
- 🌈 تم با رنگ‌های پویا و قابل تنظیم
- 📧 فرم تماس کاربردی
- 💰 بخش قیمت‌گذاری حرفه‌ای
- ❓ بخش سوالات متداول

## 🚀 راه‌اندازی سریع

### پیش‌نیازها

- Node.js (نسخه 14 یا بالاتر)
- npm یا yarn

### نصب و اجرا

```bash
# کلون کردن پروژه
git clone https://github.com/yourusername/menumita-static-site.git

# ورود به پوشه پروژه
cd menumita-static-site

# نصب وابستگی‌ها
npm install

# اجرای سرور توسعه محلی
npm run dev
```

## 🏗️ ساخت و بهینه‌سازی

برای ساخت نسخه بهینه برای انتشار:

```bash
npm run build
```

این دستور:
- فایل‌های HTML، CSS و JavaScript را بهینه می‌کند
- تصاویر را کپی می‌کند
- فایل‌ها را در پوشه `dist/` آماده می‌کند

## 🌐 استقرار در GitHub Pages

### روش 1: GitHub Actions (توصیه شده)

1. پروژه را در GitHub Push کنید
2. به تنظیمات repository بروید
3. به بخش **Pages** بروید
4. منبع (Source) را روی **GitHub Actions** تنظیم کنید
5. workflow به صورت خودکار اجرا می‌شود و سایت را deploy می‌کند

### روش 2: دستی

```bash
# ساخت نسخه بهینه
npm run build

# Push به شاخه gh-pages
git add dist/
git commit -m "Deploy to GitHub Pages"
git subtree push --prefix dist origin gh-pages
```

## 📁 ساختار پروژه

```
menumita-static-site/
├── .github/
│   └── workflows/
│       └── deploy.yml          # GitHub Actions workflow
├── images/                     # تصاویر پروژه
├── index.html                  # فایل اصلی HTML
├── styles.css                  # استایل‌های CSS
├── scripts.js                  # کدهای JavaScript
├── build.js                    # اسکریپت ساخت و بهینه‌سازی
├── package.json                # تنظیمات پروژه
└── README.md                   # این فایل
```

## 🎨 سفارشی‌سازی

### تغییر رنگ‌ها

در فایل `styles.css`، متغیرهای CSS را ویرایش کنید:

```css
:root {
  --primary-color: #2563eb;
  --secondary-color: #1e40af;
  --accent-color: #f59e0b;
  /* ... */
}
```

### تغییر محتوا

- **HTML**: فایل `index.html` را ویرایش کنید
- **CSS**: فایل `styles.css` را ویرایش کنید
- **JavaScript**: فایل `scripts.js` را ویرایش کنید

### اضافه کردن بخش‌های جدید

پروژه به صورت ماژولار طراحی شده است. می‌توانید به راحتی بخش‌های جدید اضافه کنید:

1. HTML مورد نظر را به `index.html` اضافه کنید
2. استایل‌های مربوطه را به `styles.css` اضافه کنید
3. کدهای JavaScript لازم را به `scripts.js` اضافه کنید

## 📱 ویژگی‌های واکنش‌گرا

قالب به صورت کامل برای تمامی دستگاه‌ها بهینه‌سازی شده است:

- **دسکتاپ**: 1200px و بالاتر
- **تبلت**: 768px تا 1199px
- **موبایل**: 767px و پایین‌تر

## 🔍 بهینه‌سازی SEO

قالب شامل موارد زیر برای SEO بهتر است:

- متا تگ‌های مناسب
- Schema.org structured data
- عنوان و توضیحات مناسب
- تصاویر با ویژگی alt
- لینک‌های کانونیکال

## 📧 فرم تماس

فرم تماس به صورت پیش‌فرض تنظیم شده است. برای فعال‌سازی ارسال ایمیل:

1. یک سرویس ایمیل مانند Formspree یا EmailJS انتخاب کنید
2. URL اکشن فرم را در `index.html` به‌روزرسانی کنید
3. تنظیمات مربوطه را در `scripts.js` اعمال کنید

## 🤝 مشارکت

مشارکت‌ها خوش‌آمد هستند! لطفاً:

1. Fork پروژه را انجام دهید
2. شاخه ویژگی (feature branch) بسازید
3. تغییرات خود را commit کنید
4. Push به شاخه انجام دهید
5. Pull Request ایجاد کنید

## 📄 مجوز

این پروژه تحت مجوز MIT منتشر شده است. برای اطلاعات بیشتر فایل `LICENSE` را ببینید.

## 🆘 پشتیبانی

اگر مشکلی دارید یا سوالی دارید:

1. ابتدا مستندات را بررسی کنید
2. در بخش Issues GitHub issue ایجاد کنید
3. یا از طریق ایمیل تماس بگیرید

---

**⭐ اگر این پروژه برای شما مفید بود، لطفاً آن را ستاره (star) دهید!**
